<ul class="list-group">
  <% @questions.each do |q| %>
    <li class="list-group-item">
      <%= link_to q.title, q %> - asked <%= q.created_at.strftime("%b %d '%y at %-H:%M") %> by <%= q.user.name %>
    </li>
  <% end %>
</ul>
